version: "3.8"

services:
  bot:
    build: .
    container_name: api-bot-wa
    ports:
      - "${PORT:-1997}:${PORT:-1997}"
    env_file:
      - .env
    volumes:
      - /var/www/hono-bot-wa:/app
    command: bun run --hot src/index.ts
